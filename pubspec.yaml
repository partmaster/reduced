name: reduced
description: Minimal API for the basic features of state management frameworks.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.17.3 <3.0.0'
#  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.16.6
