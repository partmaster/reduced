name: reduced
description: Uniform API for the basic features of state management frameworks.
version: 0.4.0
homepage: https://github.com/partmaster/reduced
repository: https://github.com/partmaster/reduced.git

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.22.0
